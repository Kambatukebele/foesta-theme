<section class="w-full h-fit py-10 bg-gray-100 lg:py-24 {{ extraClass }}">
  <div class="container flex flex-col justify-center items-center gap-3.5">
    <h1 class="text-xl text-center font-semibold sm:text-3xl md:text-4xl lg:text-5xl">{{ title }}</h1>
    <div class="flex flex-col justify-center items-center gap-2 sm:flex-row">
      <a href="{{ url }}" class="text-gray-500">{{ breadcrumbsOne }}</a>
      <span class="hidden sm:w-2 sm:h-2 sm:block sm:bg-gray-300 sm:rounded-full"></span>
      <h5>{{ breadcrumbsTwo }}</h5>
    </div>
  </div>
</section>
